export const actionType = {
    AUTHORIZE: 1,
    ADD: 2,
    REMOVE: 3,
    UPDATE: 4,
    VIEW: 5,
    SEARCH: 6,

}
const reducer = function (initialState, action) {
    switch (action.type) {
        case '':
            break;
    }
}

export default reducer;